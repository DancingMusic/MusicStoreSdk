{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/DancingMusic/MusicStore/main/registry/schema/official-catalog.schema.json",
  "title": "DancingMusic official connector catalog profile",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "entries", "updatedAt"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "id": { "const": "official-connectors" },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "version", "state"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
          "version": { "type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$" },
          "state": { "enum": ["publish", "withdraw"] },
          "reason": { "type": "string", "minLength": 1, "maxLength": 500 },
          "at": { "type": "string", "format": "date-time" }
        },
        "allOf": [{
          "if": { "properties": { "state": { "const": "withdraw" } } },
          "then": { "required": ["reason", "at"] },
          "else": {
            "not": {
              "anyOf": [
                { "required": ["reason"] },
                { "required": ["at"] }
              ]
            }
          }
        }]
      }
    },
    "updatedAt": { "type": "string", "format": "date-time" }
  }
}
