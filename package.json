{
  "name": "@dancingmusic/music-store",
  "version": "0.6.1",
  "description": "Connector registry, submission validation, discovery, and distribution metadata for DancingMusic",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "registry/schema",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build:package": "tsup src/index.ts --format esm,cjs --dts --clean",
    "build": "npm run build:package && node scripts/generate-registry.mjs && npm run store-service:generate",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate:registry": "npm run build:package && node scripts/generate-registry.mjs --check",
    "verify:artifacts": "node scripts/verify-artifacts.mjs",
    "store-service:generate": "node scripts/generate-store-service-publish.mjs",
    "store-service:check": "node scripts/generate-store-service-publish.mjs --check",
    "release:prepare": "npm run build && npm run store-service:check && npm run verify:artifacts",
    "check": "npm run typecheck && npm test && npm run build",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "dancingmusic",
    "music",
    "store",
    "connector-registry",
    "manifest",
    "sdk"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@dancingmusic/music-connect": "github:DancingMusic/MusicConnect#935e7719975e2fbb9a8f08b77684d76d2f131a3f"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.6"
  }
}
